/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/africa_hackathon",
  assetPrefix: "/africa_hackathon",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
